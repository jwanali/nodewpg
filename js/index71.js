let base = 1;
let result;
while (base <= 10) {
    let table = 1;
    while (table <= 10) {
        console.log (`${base} * ${table} = ${base * table}`)
        table++;
    }
    base++;
}
/**
 * table time from 1 to 10
 */